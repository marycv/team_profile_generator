const managerList = [];
const engineerList = [];
const internList = [];

// Create a function of the final HTML output
    // Master HTML template

// Create a function that generates an HTML card for Manager
    //Card HTML

// Create a function that generates an HTML card for Engineer
    //Card HTML

// Create a function that generates an HTML card for Intern
    //Card HTML

// Create a function that takes the employeeList array and split by the .getRole method
const employeesByRole = employeeList => {
    employeeList.forEach(element => {
        if( element.getRole() === "Manager" ) {
            managerList.push

           // else if
           // engineerList.push
           // else if
           // internList.push
           // else error 
        }
        
    });

    employeeList.at(0).getRole();
    

// }

    // Filter by manager and save to variable
    // Filter by engineer and save to variable
    // filter by intern and save to variable
    // console log the arrays

    // For manager array take each element and send through manager HTML function
        // Use the map method 
        // Set a variable = managerArray.map(manager => managerHTML function) 

